# Note: torch and torchvision are NOT included here
# ComfyUI already provides them configured for your hardware (CUDA/ROCm/CPU)
diffusers>=0.25.0,<0.31.0
transformers>=4.35.0
accelerate>=0.24.0
safetensors
huggingface_hub>=0.25.0
pillow
numpy
opencv-python
einops
rembg

# Additional dependencies for MV-Adapter functionality
# (We bundle the MV-Adapter pipeline code directly to avoid torch reinstall)
peft
omegaconf
controlnet_aux
